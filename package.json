{
  "name": "ab-sax-ai",
  "version": "1.0.0",
  "description": "A.B SaX AI - Think Smarter. Create Anything.",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm start --workspaces",
    "test": "npm test --workspaces",
    "setup": "npm install && npm run setup --workspaces"
  }
}
